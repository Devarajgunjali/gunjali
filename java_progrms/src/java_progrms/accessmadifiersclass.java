package java_progrms;

public class accessmadifiersclass {
	
	/*1. Default access modifier
When we do not mention any access modifier, 
it is called default access modifier. The scope of this modifier is limited to the package only. */
/*Protected data member and method are only accessible by the 
 * classes of the same package and the subclasses present in any package*/
	
	
	/*EventFiringWebdriver edriver = new EventFiringWebdriver(driver);
	 File srcFile = driver.getScreenShotAs(outputtype.file);
	 FileUtils.copyFile(srcfile, new file(imagpath));*/
	 
	 /////////////////

}
